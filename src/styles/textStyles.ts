import {StyleSheet} from 'react-native';

const textStyles = StyleSheet.create({
  heading: {
    fontSize: 18,
    color: 'black',
    fontWeight: 'bold',
    marginVertical: 8,
  },
});

export default textStyles;
