import {StyleSheet} from 'react-native';

const imageStyles = StyleSheet.create({
  avatarImage: {
    height: 60,
    width: 60,
    objectFit: 'fill',
    borderRadius: 50,
    borderColor: 'green',
    borderWidth: 4,
  },
});

export default imageStyles;
